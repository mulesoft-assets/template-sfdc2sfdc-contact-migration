<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:scripting="http://www.mulesoft.org/schema/mule/scripting" xmlns:http="http://www.mulesoft.org/schema/mule/http" xmlns:sfdc="http://www.mulesoft.org/schema/mule/sfdc" xmlns:tracking="http://www.mulesoft.org/schema/mule/ee/tracking" xmlns="http://www.mulesoft.org/schema/mule/core" xmlns:doc="http://www.mulesoft.org/schema/mule/documentation" xmlns:spring="http://www.springframework.org/schema/beans" version="EE-3.5.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-current.xsd
http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
http://www.mulesoft.org/schema/mule/ee/tracking http://www.mulesoft.org/schema/mule/ee/tracking/current/mule-tracking-ee.xsd
http://www.mulesoft.org/schema/mule/sfdc http://www.mulesoft.org/schema/mule/sfdc/current/mule-sfdc.xsd
http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
http://www.mulesoft.org/schema/mule/scripting http://www.mulesoft.org/schema/mule/scripting/current/mule-scripting.xsd">
    <flow name="mainFlow" doc:name="mainFlow" doc:description="Here you should place the main business logic.">
        <flow-ref name="gatherDataFlow" doc:name="gatherDataFlow"/>
        <flow-ref name="filterAndInsertDataFlow" doc:name="filterAndInsertDataFlow"/>
        <logger message="Amount of contacts #[flowVars['contactCount']] - Contacts Filtered #[flowVars['filteredContactsCount']]&#160;" level="INFO" doc:name="Logger"/>
        <set-payload value="Amount of contacts migrated #[flowVars['contactCount']] - Amount of contacts Filtered #[flowVars['filteredContactsCount']]" doc:name="Set Payload"/>
        <exception-strategy ref="defaultChoiceExceptionStrategy" doc:name="Reference Exception Strategy"/>
    </flow>
    <sub-flow name="gatherDataFlow" doc:name="gatherDataFlow">
        <logger message="Gathering Contact's Data..." level="INFO" doc:name="Logger"/>
		<sfdc:query config-ref="SalesforceA" query="dsql:SELECT Description, Email, FirstName, LastName, MailingCity, MailingCountry, MobilePhone, Phone, Title, Department, LastModifiedDate FROM Contact ORDER BY Email ASC" doc:name="Salesforce" fetchSize="1000"/>
        <set-variable variableName="contactCount" value="#[payload.size()]" doc:name="contactCount"/>
	</sub-flow>
    <sub-flow name="filterAndInsertDataFlow" doc:name="filterAndInsertDataFlow">
        <logger message="Starging Contacts creation&#8230;" level="INFO" doc:name="Logger"/>
        <set-variable variableName="filteredContactsCount" value="#[0]" doc:name="filteredContactsCount"/>
        <foreach doc:name="For Each">
            <enricher doc:name="Message Enricher" source="#[payload]" target="#[flowVars['contactInB']]">
                <sfdc:query-single config-ref="SalesforceB" query="#[ 'SELECT Id, LastModifiedDate FROM Contact WHERE Email = \'' + payload.get('Email') + '\'']" doc:name="Salesforce"/>
            </enricher>
            <custom-transformer class="org.mule.kicks.transformers.SFDCContactFilter" doc:name="SFDCContactFilter"/>
            <choice doc:name="Choice">
                <when expression="#[payload is NullPayload]">
                    <logger message="Contact got filtered" level="INFO" doc:name="Logger"/>
                </when>
                <otherwise>
                    <sfdc:create config-ref="SalesforceB" type="Contact" doc:name="Salesforce">
                        <sfdc:objects ref="#[payload]"/>
                    </sfdc:create>
                </otherwise>
            </choice>
        </foreach>
    </sub-flow>
</mule>
